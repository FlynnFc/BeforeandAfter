import Geuss from "@/components/Geuss";
import React from "react";
import { generate, count } from "random-words";

const page = () => {
  return (
    <div>
      <Geuss />
    </div>
  );
};

export default page;
